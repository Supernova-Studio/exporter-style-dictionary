{
  "name": "supernova-exporter-style-dictionary",
  "version": "1.0.0",
  "description": "Javascript computation for Style Dictionary exporter.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "NODE_OPTIONS=--openssl-legacy-provider npx webpack --mode=development",
    "watch": "NODE_OPTIONS=--openssl-legacy-provider npx webpack --mode=development --watch"
  },
  "author": "Supernova.io",
  "license": "MIT",
  "devDependencies": {
    "ts-loader": "^7.0.5",
    "typescript": "^3.9.9",
    "webpack": "^4.46.0",
    "webpack-cli": "^3.3.12"
  }
}
